import React from "react";

const Hero = () => {
  return (
    <section id="hero">
      <div className="hero_container">
        <div className="hero_title">
          <h1 className="hero_title_header">FORM</h1>
        </div>
        <div className="hero_cta">
          <h4>STUDIO</h4>
        </div>
      </div>
    </section>
  );
};

export default Hero;
